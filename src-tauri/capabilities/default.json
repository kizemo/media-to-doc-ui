{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability — W15-A T7: 维持 core:default 即可,LLM commands 通过 tauri::generate_handler! 默认放行(实证见 handoff §3)",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}